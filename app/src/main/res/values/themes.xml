<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AlarmingMobileApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="android:statusBarColor">@color/status</item>
        <item name="android:state_selected">@color/secondary</item>
    </style>

    <style name="Theme.AlarmingMobileApp" parent="Base.Theme.AlarmingMobileApp" />
</resources>